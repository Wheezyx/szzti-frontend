export class ConnectionInterface {
    id: string;
    ip: string;
    mac: string;
    socketNumber: string;
    vlan: string;
    patchPanel: string;
    connectionName: string;
    connectionNumber: string;
    hostId: string;
}